apiVersion: v1
kind: Service
metadata:
  name: securepen-backend-service
  labels:
    app: securepen
    tier: backend
spec:
  selector:
    app: securepen
    tier: backend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
